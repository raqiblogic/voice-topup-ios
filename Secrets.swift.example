import Foundation

// MARK: - API Keys
// Copy this file to VoiceTopup/Utilities/Secrets.swift and fill in your keys.
// The real Secrets.swift is gitignored — never commit API keys.

enum Secrets {
    /// Groq API key — get one from https://console.groq.com/keys
    static let groqAPIKey = "PASTE_YOUR_GROQ_API_KEY_HERE"
}
